SQL数据库：
    BEGIN TRANSACTION
    SELECT * FROM [dbo].[az-all-columns] WITH (TABLOCKX) ;
    WAITFOR DELAY '00:50:00'
COMMIT TRANSACTION

orcl数据库：
BEGIN
    LOCK TABLE "test" IN EXCLUSIVE MODE NOWAIT;
    DBMS_LOCK.SLEEP(10800);
END;